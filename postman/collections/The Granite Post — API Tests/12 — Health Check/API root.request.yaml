$kind: http-request
url: "{{base_url}}/api/v1/"
method: GET
scripts:
  - type: afterResponse
    code: |-
      pm.test('API root returns 200', function () {
          pm.response.to.have.status(200);
      });
    language: text/javascript
order: 2000
