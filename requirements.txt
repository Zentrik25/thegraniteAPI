# Core
Django>=6.0,<7.0
djangorestframework>=3.15,<4.0

# Auth
djangorestframework-simplejwt>=5.3,<6.0

# Schema / docs
drf-spectacular>=0.27,<1.0

# CORS
django-cors-headers>=4.3,<5.0

# Database
psycopg2-binary>=2.9,<3.0
dj-database-url>=2.1,<3.0

# Cache / broker (Redis)
redis>=5.0,<6.0

# HTTP client — required by core.cloudflare_purge at runtime
requests>=2.31,<3.0

# Async task queue — required at startup (settings.py imports celery.schedules)
celery>=5.3,<6.0

# Image processing — required by media_assets validators and tests
Pillow>=10.0,<13.0

# Web-push notifications — required by notifications.tasks at runtime
pywebpush>=1.14,<3.0

# S3 / Cloudflare R2 media storage (activated via USE_S3=true in production)
django-storages>=1.14,<2.0
boto3>=1.28,<2.0

# Payment gateway — required by subscriptions.paynow_client at runtime
paynow>=1.0,<2.0

# Static files
whitenoise>=6.7,<7.0

# Production WSGI server
gunicorn>=22.0,<24.0
